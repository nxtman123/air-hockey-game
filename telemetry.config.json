{
  "name": "Air Hockey",
  "version": "2026.6.3",
  "description": "A silly game for the trade show",
  "logoPath": "public/assets/icon.png",
  "thumbnailPath": "public/assets/thumbnail.png",
  "mountPoints": {
    "render": "/render"
  },
  "devServer": {
    "runCommand": "pnpm install && vite --port $PORT",
    "url": "http://localhost:$PORT",
    "readyPattern": "VITE.*ready in"
  }
}
